import React, { useState } from 'react';
import FormAuth from '../../components/other/FormAuth';
import LayoutAuth from '../../layout/LayoutAuth';

const Nmqmk = () => {
  const [init, setInit] = useState(""); // Initialize the state

  return (
    
  );
};

export default Nmqmk;